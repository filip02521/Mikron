# Skopiuj poniższe linie do .env.local na komputerze w sieci firmowej (LAN).
# Wymagane: komputer z npm run dev musi widzieć host Subiekta (ping / curl).

# Główne API — codzienna praca (live, baza MIKRAN).
SUBIEKT_API_BASE_URL=http://192.168.0.140:5080/api/v1
SUBIEKT_API_AUTH_MODE=none
SUBIEKT_API_HEALTH_PATH=/health
SUBIEKT_API_TIMEOUT_MS=15000

# WYMAGANE dla szacunku ZD (/zakupy/szacunek) + create ZD.
# Dozwolone porty: :5080 (LIVE / aktualna baza) albo :5082 (testowa kopia).
# NA RAZIE: live :5080 — snapshoty z host_kind=live. Create ZD zapisuje prawdziwe dokumenty.
# Powrót na sandbox: ustaw :5082 gdy kopia testowa wstanie.
# Bez tej linii szacunek jest ZABLOKOWANY (zero cichego fallbacku na BASE_URL).
SUBIEKT_API_ORDERS_BASE_URL=http://192.168.0.140:5080/api/v1
SUBIEKT_API_ORDERS_TIMEOUT_MS=60000

# Klucz API wyłączony u Ciebie — nie ustawiaj SUBIEKT_API_KEY.
# Gdy włączysz klucz na serwerze Subiekta:
# SUBIEKT_API_KEY=twoj-klucz
# SUBIEKT_API_AUTH_MODE=bearer
